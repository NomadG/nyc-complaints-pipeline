[project]
name = "spark-tranformation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "delta-spark>=4.2.0",
    "jupyter>=1.1.1",
    "pandas>=3.0.2",
]
